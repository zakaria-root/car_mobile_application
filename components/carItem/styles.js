import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    carContainer: {
        width: '100%',
        height: '100%',
      },
      titles: {
        marginTop: "30%",
        textAlign: 'center',
      },
      title: {
        fontSize: 35,
        fontWeight: '500'
      },
      image:{
        width : '100%',
        height: '100%',
        resizeMode: 'cover',
        position: 'absolute'
      },
});

export default styles;